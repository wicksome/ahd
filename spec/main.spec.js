"use strict";
import "testHelper.js";

const expect = require("chai").expect,
	should = require("chai").should(),
	path = require("path"),
	colors = require("colors"),
	fs = require("fs");

// Scenario 1: Refunded items should be returned to stock
// Given a customer previously bought a black sweater from me
// And I currently have three black sweaters left in stock
// When he returns the sweater for a refund
// Then I should have four black sweaters in stock

describe("Check ahd's config", function() {
	it("should check exist a config", done => {
		done();
	});
	it("should initialize a config if none config");
	it("should check exist httpd.conf if exist config");
	it("should show command that initialize config when not exist the httpd.conf");
});

describe("", function() {

});
