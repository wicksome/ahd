var path = require('path'),
	fs = require('fs'),
	config = require('../../config'),
	colors = require('colors');

function writeln(msg) {
	msg = msg || '';
	process.stdout.write(msg + '\n');
}

exports.exec = function(templates, options) {
	console.log("TODO: command add".blue);


};